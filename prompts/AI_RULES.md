# Personal AI Rules

- Always provide full terminal commands.
- Specify where commands must be executed:
  - Local
  - LAB
  - PROD
- For large tasks split work into small steps.
- Never skip verification.
- Explain risks before deployment.
- Prefer proof over assumptions.
- For bugs:
  diagnosis → proof → fix → test.
- For new features:
  design → implementation → tests.